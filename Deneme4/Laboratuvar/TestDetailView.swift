//
//  TestDetailView.swift
//  Deneme4
//
//  Created by Eren on 13.06.2024.
//


/*
import SwiftUI

struct TestDetailView: View {
    var test: Test

    var body: some View {
        VStack(alignment: .leading) {
            Text("Hasta Adı: \(test.patient.general.name)")
                .font(.headline)
            Text("TC Kimlik No: \(test.patient.general.tcNo)")
                .font(.subheadline)
            Text("Test Tipi: \(test.testType.testType.rawValue)")
                .font(.subheadline)
            Text("Durum: \(test.status.status.rawValue)")
                .font(.subheadline)
            Text("Numune Tipi: \(test.sampleType.sampleType.rawValue)")
                .font(.subheadline)
            // Daha fazla bilgi ekleyebilirsiniz
        }
        .padding()
        .navigationTitle("Test Detayları")
    }
}
*/
